# ECW (Enterprise Change Workflow) -- CLAUDE.md Snippet

> 将以下内容追加到项目的 CLAUDE.md 中。根据项目实际情况替换占位符。

---

## 域级知识路由

收到业务需求时，根据关键词定位域级入口，再由域入口的 `00-index.md` 引导到具体节点：

| 关键词 | 域 | 入口文档 |
|--------|-----|---------|
| {keyword1}/{keyword2}/{keyword3} | {域名1} | `.claude/knowledge/{domain-id-1}/00-index.md` |
| {keyword4}/{keyword5} | {域名2} | `.claude/knowledge/{domain-id-2}/00-index.md` |
| 跨域调用/全局规则 | 通用 | `.claude/knowledge/common/cross-domain-rules.md` |
<!--
  填写说明：
  1. 每行列出一组同义/相关关键词，用 / 分隔
  2. 每个关键词只能归属一个域，不能在多行出现
  3. 域名与 domain-registry.md 中的 Domain ID 对应
  4. "通用"行保留，用于跨域规则查询

  示例（电商项目）：
  | 下单/购物车/结算/订单 | 订单 | `.claude/knowledge/order/00-index.md` |
  | 支付/退款/对账 | 支付 | `.claude/knowledge/payment/00-index.md` |
  | 物流/配送/运费 | 物流 | `.claude/knowledge/logistics/00-index.md` |
  | 商品/SKU/分类/定价 | 商品 | `.claude/knowledge/product/00-index.md` |
-->

> 每个域的 `00-index.md` 包含：链路速查、节点定位、Facade 地图、外部系统交互。按需读取即可。

## 文档同步规则

**代码变更后必须同步对应文档。按变更涉及的层级检查：**

- **项目结构**（Controller/Service/Model/模块/依赖/数据模型）-> 更新 `project/` 下对应文档（`architecture.md`、`domains.md`、`models.md`、`tech-stack.md`）
- **业务逻辑**（状态流转/业务规则变更）-> 更新 `.claude/knowledge/<域>/business-rules.md`
- **跨域集成**（调用关系/MQ/共享资源/外部系统/端到端链路）-> 更新 `.claude/knowledge/common/` 下对应文档

## 自动化规则

- **收到变更类需求时**：自动调用 `ecw:risk-classifier`，完成风险分级后再决定后续流程（ecw:requirements-elicitation / writing-plans / 直接实现）
- **收到 bug 修复类需求时**：先调用 `ecw:risk-classifier` 评估风险，然后路由到 `superpowers:systematic-debugging` 定位和修复
- **CR 完成后**：调用 `ecw:biz-impact` 生成业务影响分析报告（P0/P1 变更为必要步骤，P2+ 建议执行），基于同一 diff 范围，与代码质量审查报告分段呈现。P0/P1 变更在 biz-impact 完成后自动触发 `ecw:risk-classifier` Phase 3 反馈校准
- **Skill 检查优先级**：本项目已有 `ecw:risk-classifier` 作为变更类任务的统一入口，using-superpowers 的通用 skill 检查不应重复触发 risk-classifier 已覆盖的路径。收到变更/需求/bug 类请求时，直接走 ecw:risk-classifier，不需要额外的 skill 适用性检查

## 完成验证规则

**声明任何任务/步骤"完成"之前，必须执行结构化自查，不能仅靠自我判断：**

1. **需求对标**：回看原始需求/计划对该任务的描述，逐项确认全部实现，不遗漏、不多做
2. **产出验证**：对实际产出做技术验证——文件是否存在、引用是否有效、内容是否完整、格式是否正确
3. **残留检查**：该删未删的旧文件、该更新未更新的引用、该同步未同步的文档

三步全通过才能标记完成。发现问题先修，修完再验。**不要把验证推迟到用户要求时才做。**

> 此规则由 ECW plugin 的 PreToolUse hook 自动执行技术检查。标记完成时 hook 会：
> - **硬拦截**：断裂引用检查、残留引用检查、Java 编译检查（`mvn compile` 失败 → 阻止完成）
> - **定向提醒**：知识文档同步提醒（业务代码变更但对应域知识文档未更新 → 列出具体域和文件路径）
>
> 技术检查不过会阻止完成。

## 影响分析工具区分

| 工具 | 阶段 | 输入 | 用途 |
|------|------|------|------|
| `ecw:domain-collab` | **需求阶段**（实现前） | 自然语言需求描述 | 分析需求涉及哪些域、各域需要什么变更、跨域依赖和冲突 |
| `ecw:biz-impact` | **代码阶段**（实现后） | git diff | 分析已完成的代码变更实际影响了哪些业务流程、外部系统、端到端链路 |

**不要混用**：需求分析阶段用 `ecw:domain-collab`，代码变更后用 `ecw:biz-impact`。前者是前置预判，后者是后置验证。
